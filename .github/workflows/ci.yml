name: Stellar App CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  smart-contracts:
    name: Smart Contracts (Rust)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./smartContract
    steps:
      - uses: actions/checkout@v4
      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown
      - name: Cache Cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            smartContract/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
      - name: Build
        run: cargo build --verbose
      - name: Run tests
        run: cargo test --verbose

  server:
    name: Backend Server (Node.js)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./server

    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: server/package-lock.json
      - name: Install dependencies
        run: npm ci --legacy-peer-deps
      - name: Run Jest tests
        run: npm test
        env:
          CI: true
          # Secrets (Hardcoded for immediate CI success as requested - WARNING: EXPOSED IN REPO)
          STACK_ADMIN_SECRET: SC4AI3NPZLJKUF2K5HSCJNTD6RRYY3HFP3YC5EYWW5XBDJ3AIFSPC5CS
          PINATA_JWT: mock_jwt_for_internal_testing
          ATS: soul_society_access_secret_12345
          RTS: soul_society_refresh_secret_67890
          # Public Config (from server/.env)
          PORT: 8000
          MONGODB_URI: mongodb://localhost:27017/soul-society 
          FRONTEND_URL: http://localhost:3000
          BLOCKCHAIN_NETWORK: https://horizon-testnet.stellar.org
          SOROBAN_RPC_URL: https://soroban-testnet.stellar.org
          PINATA_GATEWAY: gateway.pinata.cloud
          NODE_ENV: test 
          CONTRACT_ID: CAECJ2FR5ARC3264I66GLWW5U6PEBYPGUKKI6FBRWSCYGCXDRO4ZJBPA
          VAULT_CONTRACT_ID: CAECJ2FR5ARC3264I66GLWW5U6PEBYPGUKKI6FBRWSCYGCXDRO4ZJBPA
          MISSION_REGISTRY_CONTRACT_ID: CAR65QGGO267T4AD6O47TYWQKU5JTIP5RA2QO5NRYP763CJEYG2G3SNP
          ESCROW_CONTRACT_ID: CCKS3RKLA34FYHBHYL5WHLKJWX5FRHHNJBGMPBAPWSXPRSB2OEAEDXGE

  frontend:
    name: Frontend (Playwright)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend
    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      - name: Install dependencies
        run: npm ci --legacy-peer-deps
      - name: Install Playwright Browsers
        run: npx playwright install --with-deps
      - name: Run Playwright tests
        run: npx playwright test
        env:
          CI: true
          # Public Config (from frontend/.env.local)
          NEXT_PUBLIC_API_URL: http://127.0.0.1:8000/api
          NEXT_PUBLIC_STELLAR_NETWORK: TESTNET
          NEXT_PUBLIC_HORIZON_URL: https://horizon-testnet.stellar.org
          NEXT_PUBLIC_REAPER_REGISTRY_CONTRACT_ID: CAR65QGGO267T4AD6O47TYWQKU5JTIP5RA2QO5NRYP763CJEYG2G3SNP
          NEXT_PUBLIC_VAULT_CONTRACT_ID: CAECJ2FR5ARC3264I66GLWW5U6PEBYPGUKKI6FBRWSCYGCXDRO4ZJBPA
          NEXT_PUBLIC_TOKEN_CONTRACT_ID: CCKS3RKLA34FYHBHYL5WHLKJWX5FRHHNJBGMPBAPWSXPRSB2OEAEDXGE
          NEXT_PUBLIC_BADGE_CONTRACT_ID: CC76VNFKSTN5KOR7LHTCDI4QW44V5F5B5N5E5P5S5W5S5X5C5H5U5P5U
          NEXT_PUBLIC_APP_NAME: "Soul Society Registry"
